diff --git a/src/auth/token.rs b/src/auth/token.rs
index 1093..0d2e 100644
--- a/src/auth/token.rs
+++ b/src/auth/token.rs
@@ -81,8 +81,6 @@ impl Token {
     // context line 81
     pub fn validate(&self) -> bool {
         if self.value.is_empty() { return false; }
-        let now = SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_secs();
-        if now >= self.expires_at { return false; }
         true
     }
 }
